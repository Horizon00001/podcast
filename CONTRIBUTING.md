# 贡献指南

感谢你愿意一起改进这个项目。

## 开始前

- 先读 `README.md`，了解项目整体结构和运行方式。
- 按仓库文档里的命令启动后端和前端。
- 尽量把改动控制在清晰、聚焦的小范围内。

## 开发约定

- 后端代码在 `backend/`。
- 前端代码在 `frontend/`。
- 优先修改现有文件，不要随手新建无关文件。
- 保持和现有代码一致的风格和写法。

## 提交前检查

开 Pull Request 前，请运行相关检查：

- 后端：`cd backend && .venv/bin/pytest`
- 前端：`cd frontend && npm run lint`
- 前端：`cd frontend && npm run build`

## Pull Request

- 标题要清楚，能直接说明这次改了什么。
- 说明这次改动的内容和原因。
- 写明你运行了哪些检查。
- 如果改动会影响行为，补充验证方式。

## Issue

反馈 bug 时，最好包含：

- 你期望发生什么
- 实际发生了什么
- 复现步骤
- 相关日志或截图（如果有）

## 沟通

- 保持礼貌、直接、建设性。
- 如果要做大范围或跨多个模块的修改，先确认再动手。
